/**
 * INK DIFFUSION HOOKS
 *
 * Custom hooks for the Ink Diffusion system.
 */

export {
  useGenerationSimulation,
  BUDGET_STAGES,
  STANDARD_STAGES,
  PREMIUM_STAGES,
} from './useGenerationSimulation';

export type {
  Stage,
  GenerationState,
  UseGenerationSimulationOptions,
} from './useGenerationSimulation';
